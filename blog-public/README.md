# Pxczxn Blog Public

公开博客前台，基于 React + Vite + Tailwind CSS。

## 本地开发

```bash
npm install
npm run dev
```

默认端口：`4000`。

## 构建

```bash
npm run lint
npm run build
```

生产 API 地址通过 `VITE_API_BASE_URL` 配置；如果前端和后端同域部署，可以保持为空或使用同域反向代理。
